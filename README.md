# knip-git-preprocessor
Preprocessor for Knip that filters out files recently touched by git

This is a Work In Progress preprocessor to Knip, to filter out files that have been recently modified by git.